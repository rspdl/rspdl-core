@모듈 프로필(profile)

사용자 프로필(user_profile)은 다음 필드들로 구성되어 있다.
    표시 이름(display_name): 필수 문자열

프로필 작성 화면(profile_input)에서는 사용자 프로필을 생성할 수 있다.
프로필 작성 화면(profile_input)에서는 사용자 프로필의 표시 이름을 입력할 수 있다.
프로필 상세 화면(profile_detail)에서는 사용자 프로필의 표시 이름을 조회할 수 있다.
