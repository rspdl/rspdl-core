@모듈 주문(ordering)

주문(order)은 다음 필드들로 구성되어 있다.
    상태(status): 필수 문자열

주문 기록(log)은 다음 필드들로 구성되어 있다.
    메모(note): 필수 문자열

주문 작성 화면(create_order)에서는 주문을 생성할 수 있다.
기록 작성 화면(create_log)에서는 주문 기록을 생성할 수 있다.
주문 취소(cancel_order)는 행동이다.
주문 취소가 실행되면 주문을 수정한다.
주문 취소가 실행되면 주문 기록을 삭제한다.
