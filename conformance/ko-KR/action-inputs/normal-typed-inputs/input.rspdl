@모듈 주문(ordering)

주문 상태(order_status)는 다음 값 중 하나다.
    접수됨(received)
    취소됨(cancelled)

주문(order)은 다음 필드들로 구성되어 있다.
    상태(status): 필수 주문 상태

주문 접수(receive_order)는 행동이다.
주문 접수는 기존 주문을 대상 주문(order)으로 입력받는다.
주문 접수는 문자열을 접수 사유(reason)로 입력받는다.
주문 접수는 주문 상태를 요청 상태(status)로 입력받는다.
